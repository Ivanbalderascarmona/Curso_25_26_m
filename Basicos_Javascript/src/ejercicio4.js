// //usos de los arrays
// //declaracion:
// const edades = [];
// const frutas = ["pera", "manzana", "fresa"];

// // usando el constructor

// const cp = new Array();
// const cc = new Array("gsgsdfg", "dfwgwrg","egehtehedsgf");

// // añadir elementos
// edades.push(10);//añade al final
// edades.unshift(8);//añade al principio

// // eliminar elementos

// edades.pop();//elimina el ultimo y retorna lo que ha eliminado
// edades.shift();//elimina el primero y retorna lo que ha eliminado

// // ***** slide extraer partes de un array

// edades.slice(0,2);

// // ************** map

// edades.map((edad) => edad * 2);

// // ************** filter

// edades.filter((edad) => edad >= 18);


//dado un array de nombres, crear una funcion llamada mayusculas que ponga en mayusculas todos los elementos del array que le paso por parametro
//crea una funcion llamada precios con iva que al pasarle un array de precios, me los devuelva con el iva incluido iva = 21%
//crear una funcion llamada impares cuadrado, que le pase un array de números y me devuelva solo los impares elevados al cuadrado
//crear una funcion llamada normalizarEmail que le pase un array de email que pueden llevar espacios al principio y alfinal del email y me quite los espacios al principio y al final
//crear una funcion llamada filtrarLongitud, que le pase como parametro un array de nombres, tamaño y me devuelva solo los nombres cuya longitud es mayor o igual al parametro dado
//Normalizar nombres propios que le pase como parametro un array de nombres y me los devuelva con la letra de cada palabra en mayuscula
