import db from './db.js';

// -ejemplicicar un carrito de la compra persistente usando closure y estableciendo la persistencia a 
// traves de una base de datos sqlite-3
// que permita insertar productos, insertar cantidad de productos , eliminar productos y calcular total
const createCart = () => {
    const addProducto = () => {
        
    }
}